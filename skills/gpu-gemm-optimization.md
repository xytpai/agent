# GPU / GEMM 优化 Skill

适用于 GPU kernel 性能优化与根因分析，尤其是 FlyDSL/ROCm/gfx950 GEMM、FP8/BF16、HTI、full-tile、split-K/slice-K、LDS swizzle、ASM/trace 分析及无回归重构。

完整技能入口：[SKILL.md](gpu-gemm-optimization/SKILL.md)。

使用时先读取该入口，再按任务读取 references；scripts、examples、tests 均位于同一技能目录，文档相对链接保持不变。
